<div>
    <p>Hola {{ nombre }}, tus datos son:</p>
    <ul>
        <li>{{ nombre }}, {{ apellido }}</li>
        <li>Edad: {{ edad }}</li>
        <li>Email: {{ correo }}</li>
    </ul>
    {{#if isAdmin }}
        <ul>
        {{#each foods}}
            <li>{{this.name}}: {{this.price}}</li>
        {{/each}}
        </ul>
    {{else}}
        <p>No tienes permiso para ver la lista de foods</p>
    {{/if}}
</div>